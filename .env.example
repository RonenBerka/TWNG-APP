# ====================================
# TWNG Environment Variables
# ====================================
# Copy this file to .env and fill in values:
#   cp .env.example .env

# ----- Supabase (Required) -----
# Get from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE

# ----- Email (Optional — emails are sent via Edge Function) -----
# The send-email Edge Function handles email delivery server-side.
# Set RESEND_API_KEY as a Supabase secret, NOT here.
# These are only used for frontend display/config:
VITE_EMAIL_FROM=TWNG <hello@twng.com>
VITE_EMAIL_REPLY_TO=support@twng.com

# ----- Site URL (Optional) -----
# Used for email links and OG tags. Defaults to window.location.origin
VITE_BASE_URL=https://twng.com
